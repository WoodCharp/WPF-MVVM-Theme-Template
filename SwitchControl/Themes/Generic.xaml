<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:SwitchControl">

    <Style TargetType="{x:Type local:Switch}">

        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Width" Value="50"/>
        <Setter Property="Height" Value="25"/>

        <Setter Property="Template">
            <Setter.Value>

                <ControlTemplate TargetType="{x:Type local:Switch}">
                    <Border Background="Transparent" BorderBrush="{TemplateBinding Foreground}"
                            BorderThickness="1.5" CornerRadius="12.5">

                        <Canvas>
                            <Ellipse x:Name="ellipse" Canvas.Left="0" HorizontalAlignment="Left"
                                     Height="18" Width="18" VerticalAlignment="Center" Margin="1.5, 1.75, 0, 0"/>
                        </Canvas>
                    </Border>
                </ControlTemplate>

            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>